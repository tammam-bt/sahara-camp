# OS
.DS_Store
Thumbs.db
desktop.ini
ehthumbs.db

# Editor
.vscode/
.idea/
*.swp
*.swo
*.sublime-workspace
*.sublime-project
.cursor/
AGENTS.md

# Node (if you ever add a build step)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.log

# Netlify CLI build artifacts (netlify.toml itself IS committed)
.netlify/
dist/

# Supabase (local CLI state, if re-introduced)
.supabase/

# Environment files — NEVER commit these
.env
.env.local
.env.*.local
# Note: .env.example is NOT ignored (it's a safe template to commit)
